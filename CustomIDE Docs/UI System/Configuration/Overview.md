The UI layout and colour scheme can both be configured separately using different configuration files. Within the folder structure

```
CustomIDE-Vulkan/
	├─ assets/
		├─ layout/ <- add a.ui file here for a layout
		├─ textures/
		├─ themes/ <- add a .th file here for a theme
```
